# Inbox

> Raw input queue. Dump anything here — ideas, bugs, screenshots, links.

- The task list doesn't sort by priority in the UI — it just shows creation order
- Add a "clear completed" button to the done column
- Would be nice to have dark/light mode toggle
- Screenshot: the health endpoint returns 200 even when the DB file is missing — should return 503
